spring:
  application:
    name: mall-gateway
  cloud:
    nacos:
      config:
        server-addr:  127.0.0.1:8848
        namespace: dev
        group: DEFAULT_GROUP
        refresh-enabled: true
        file-extension: yml